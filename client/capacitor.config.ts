import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.nitrotap.brainlift',
  appName: 'brain-lift',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
